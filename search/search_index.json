{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"LAMig","text":"<p>This is the group blog of team LAMig of CS 191 WFR1 2526.1.</p>"},{"location":"#members","title":"Members","text":"<ul> <li> <p>Lucas Agcaoili (lvagcaoili@up.edu.ph)</p> </li> <li> <p>Miguel Guiang (maguiang@up.edu.ph)</p> </li> <li> <p>Aeron Dann Penaflorida (appenaflorida@up.edu.ph)</p> </li> </ul>"},{"location":"#pdf-links-to-deliverables","title":"PDF Links to Deliverables","text":"<ul> <li> <p>Project Plan: SALN App Project Plan.pdf</p> </li> <li> <p>Course Agreement: Course-Agreement.pdf</p> </li> <li> <p>System Requirements Document: SALN App System Requirement Document.pdf</p> </li> <li> <p>Demo Video 1: https://drive.google.com/file/d/1tHjtdQTW0bIAL3QyiqjedKMkCo-imvpZ/view?usp=sharing</p> </li> <li> <p>Review Document (Reviewed: Group Power Puff Boys): Review Document.pdf</p> </li> <li> <p>Software Test Report (Reviewed By Power Puff Boys): Software Test Report.pdf</p> </li> <li> <p>Progress Report 1: CS 192 Progress Report 01-30-2026.pdf</p> </li> <li> <p>Software Maintenance Plan: CS 192 Software Maintenance Plan</p> </li> <li> <p>Progress Report 2: CS 192 Progress Report 02-06-2026.pdf</p> </li> <li> <p>Progress Report 3: CS 192 Progress Report 02-13-2026.pdf</p> </li> <li> <p>Progress Report 4: CS 192 Progress Report 02-19-2026.pdf</p> </li> <li> <p>Progress Report 5: CS 192 Progress Report 02-27-2026.pdf</p> </li> <li> <p>1st Project Evaluation by Client: Project_Evaluation_by_Client_1.pdf</p> </li> <li> <p>Progress Report 6: CS 192 Progress Report 03-05-2026.pdf</p> </li> <li> <p>Progress Report 7: CS 192 Progress Report 03-12-2026.pdf</p> </li> <li> <p>Review Document (Reviewed TLE): Review Document 03-19-2026.pdf</p> </li> <li> <p>Software Test Report (Reviewed TLE) Software Test Report 03-19-2026.pdf</p> </li> <li> <p>Progress Report 8: CS 192 Progress Report 03-19-2026.pdf</p> </li> <li> <p>Progress Report 9: CS 192 Progress Report 03-27-2026.pdf</p> </li> <li> <p>Progress Report 10: CS 192 Progress Report 04-10-2026.pdf</p> </li> <li> <p>2nd Project Evaluation by Client: Project_Evaluation_by_Client_2.pdf</p> </li> <li> <p>Progress Report 11: CS 192 Progress Report 04-17-2026.pdf</p> </li> <li> <p>Software Test Report (Reviewed Thomas &amp; Friends) Software Test Report 04-28-2026.pdf</p> </li> </ul>"},{"location":"Backup/","title":"Backup/Reinstall Procedures","text":""},{"location":"Backup/#backup-the-database","title":"Backup the Database","text":"<p>The VM is configured to create database backups daily. You could access the compressed backups at <code>var/backups/mysql</code>. The backups will each last for one month.</p> <p>You could choose a backup, unzip it, and then import it into MySQL. <pre><code>gunzip &lt; saln_app_DB_MM_DD_YYYY.sql.gz &gt; saln_app_DB_MM_DD_YYYY.sql\nmysql -u &lt;USER&gt; -p saln_app_DB &lt; saln_app_DB_MM_DD_YYYY.sql\n</code></pre></p>"},{"location":"DOSetup/","title":"Digital Ocean VM Setup","text":"<p>This page would be for setting up domains, installing dependencies, and setting up the Digital Ocean droplet. </p> <p>This assumes the use of:</p> <ul> <li>Digital Ocean Droplet (Ubuntu 24)</li> <li>Netim (this is used for getting a domain)</li> <li>Mailgun (this is used for sending emails)</li> </ul> <p>You should also get these from the developers: </p> <ul> <li><code>saln-server/.env</code></li> <li>private <code>saln_ssh</code> and public <code>saln_ssh.pub</code> SSH key pair from the developers. Save these in <code>~/.ssh</code>.</li> </ul> <p>NOTE: Instructions for setting up the production repository will be in Updating Production</p>"},{"location":"DOSetup/#digital-ocean-droplet-access","title":"Digital Ocean Droplet Access","text":"<p>There are two ways of accessing the Digital Ocean droplet.</p> <ol> <li> <p>You use the console on the Digital Ocean dashboard.</p> </li> <li> <p>On your terminal, you run the command <code>ssh -i ~/.ssh/saln_ssh root@178.128.117.229</code>. To simplify this, you could run <code>sudo nano ~/.ssh/config</code> and then put: <pre><code>Host saln-do\n    Hostname 178.128.117.229\n    User root\n    IdentityFile ~/.ssh/saln_ssh\n</code></pre> Then run <code>ssh -A saln-do</code>.</p> </li> </ol>"},{"location":"DOSetup/#netim-domain","title":"Netim Domain","text":"<p>From Netim, the development team got the domain <code>lamig-saln.online</code>. This domain will be valid up until February 9, 2027, but could be renewed.</p> <p>We will also assign subdomain <code>mg.lamig-saln.online</code> for Mailgun's mailing.</p>"},{"location":"DOSetup/#mailgun-mailing-service","title":"Mailgun Mailing Service","text":"<p>For mailgun, we used the sending domain <code>mg.lamig-saln.online</code>. Then, we copied its DNS Records given by Mailgun into the DNS records of <code>lamig-saln.online</code> over on Netim. The sending domain is now setup.</p> <p>Since SMTP is blocked on Digital Ocean, we will be using the Mailgun API. The API key was generated on the Mailgun website and is currently being used in the <code>saln-server/.env</code> file as <code>MAILGUN_SECRET</code>. The option to generate an new API key could be seen on the Mailgun dashboard. </p> <p>In <code>saln-server/.env</code>, we now use these fields for mailing: <pre><code>MAIL_MAILER=mailgun\nMAIL_FROM_ADDRESS=saln_app@mg.lamig-saln.online\nMAIL_FROM_NAME=\"${APP_NAME}\"\nMAILGUN_DOMAIN=mg.lamig-saln.online\nMAILGUN_SECRET=&lt;API-KEY&gt;\nMAILGUN_ENDPOINT=api.mailgun.net\n</code></pre></p>"},{"location":"DOSetup/#firewall-setup","title":"Firewall Setup","text":"<p>There are two layers to the Firewall.</p> <p>On the Networking tab of the Digital Ocean Droplet's dashboard, we setup limited the open inbound ports:</p> Type Protocol Port Range Sources SSH TCP 22 All IPv4, All IPv6 HTTP TCP 80 All IPv4, All IPv6 HTTPS TCP 443 All IPv4, All IPv6 <p>We also setup the outbound ports:</p> Type Protocol Port Range Sources ICMP ICMP All IPv4, All IPv6 All TCP TCP All ports All IPv4, All IPv6 All UDP UDP All ports All IPv4, All IPv6 <p>Then, on the terminal, we also run this code block to setup the firewall on the virtual machine: <pre><code>sudo ufw default deny incoming\nsudo ufw default allow outgoing\nsudo ufw allow 22\nsudo ufw allow 80\nsudo ufw allow 443\nsudo ufw enable\n</code></pre></p> <p>With this setup, our virtual machine should only take in SSH, HTTP, and HTTPS messages on ports 22, 80, and 443, respectively.</p>"},{"location":"DOSetup/#installing-git","title":"Installing Git","text":"<p>Install git and then clone the code repository.  <pre><code>sudo apt install git -y\n</code></pre></p>"},{"location":"DOSetup/#installing-npm","title":"Installing npm","text":"<p>To install npm, the package manager of Node.js, get it here: https://nodejs.org/en/download <pre><code># Download and install nvm:\ncurl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash\n\n# in lieu of restarting the shell\n\\. \"$HOME/.nvm/nvm.sh\"\n\n# Download and install Node.js:\nnvm install 24\n\n# Verify the Node.js version:\nnode -v # Should print \"v24.13.0\".\n\n# Verify npm version:\nnpm -v # Should print \"11.6.2\".\n</code></pre></p>"},{"location":"DOSetup/#process-manager","title":"Process Manager","text":"<p>For this setup, we have the MarkoJS client served by a Node server. This is usuall done by <code>npm run build</code>, then <code>node dist/index.mjs</code>. But, this disconnects once the terminal gets terminated. Process Manager 2 (<code>pm2</code>) allows us to keep this process alive. <pre><code>npm install -g pm2\npm2 -v\n</code></pre></p> <p>Also, we would want to configure the <code>swapfile</code> as so: <pre><code>fallocate -l 2G /swapfile\nchmod 600 /swapfile\nmkswap /swapfile\nswapon /swapfile\necho '/swapfile none swap sw 0 0' &gt;&gt; /etc/fstab\n</code></pre></p>"},{"location":"DOSetup/#installing-php-84","title":"Installing PHP 8.4","text":""},{"location":"DOSetup/#php-84","title":"PHP 8.4","text":"<p>To install PHP 8.4: <pre><code>sudo apt install -y software-properties-common\nsudo add-apt-repository ppa:ondrej/php -y\nsudo apt update\nsudo apt install php8.4 php8.4-cli php8.4-common php8.4-mbstring php8.4-xml php8.4-curl php8.4-mysql php8.4-zip php8.4-gd php8.4-intl php8.4-fpm php8.4-bcmath unzip -y\n</code></pre></p>"},{"location":"DOSetup/#composer","title":"Composer","text":"<p>We would also need to install Composer, PHP's package manager: <pre><code>sudo apt install composer\n</code></pre></p>"},{"location":"DOSetup/#laravel","title":"Laravel","text":"<p>Laravel is the PHP Framework to be used for the backend server side of the web app. <pre><code>composer global require laravel/installer\necho 'export PATH=\"$PATH:$HOME/.config/composer/vendor/bin\"' &gt;&gt; ~/.bashrc\nsource ~/.bashrc\n</code></pre></p>"},{"location":"DOSetup/#database-setup","title":"Database Setup","text":""},{"location":"DOSetup/#install-mysql","title":"Install MySQL","text":"<p>To install MySQL Server: <pre><code>sudo apt update\nsudo apt install mysql-server -y\n</code></pre></p> <p>You might want to keep note of these commands to do some sanity checks for the MySQL server:</p> <ul> <li>To start MySQL Server: <code>sudo service mysql start</code> </li> <li>To check status: <code>sudo service mysql status</code> </li> <li>To login locally: <code>sudo mysql -u root -p</code> </li> <li>To stop MySQL Server: <code>sudo service mysql stop</code></li> </ul> <p>From here, you could do MySQL queries directly to the database. Do note though that the source code uses the <code>Crypt</code> library of Laravel to encrypt stored database entries.</p>"},{"location":"DOSetup/#database-initialization","title":"Database Initialization","text":"<p>Create <code>saln_app_DB</code> in MySQL and a user for Laravel migrations to use. <pre><code>sudo mysql -u root -p\n</code></pre> <pre><code>CREATE DATABASE saln_app_DB;\nCREATE USER 'saln_user'@'localhost' IDENTIFIED BY '&lt;password&gt;';\nGRANT ALL PRIVILEGES ON saln_app_DB.* TO 'saln_user'@'localhost';\nFLUSH PRIVILEGES;\n</code></pre></p> <p>Note that <code>&lt;password&gt;</code> must be kept secret. The current <code>&lt;password&gt;</code> is found in <code>saln-server/.env</code> as the <code>DB_PASSWORD</code> field for production. </p>"},{"location":"DOSetup/#automatic-backups","title":"Automatic Backups","text":"<p>Compressed backup files will live in <code>/var/backups/mysql</code>. <pre><code>mkdir /var/backups/mysql\n</code></pre></p> <p>Then, we will make a bash script that creates a compressed backup of the database. <pre><code>sudo nano /usr/local/bin/saln_app_db_backup.sh\n</code></pre> <pre><code>#!/bin/bash\n\n# CONFIGURATION\nDB_USER=\"&lt;USER&gt;\"\nDB_PASSWORD=\"&lt;PASSWORD&gt;\"\nDB_NAME=\"saln_app_DB\"\nBACKUP_DIR=\"/var/backups/mysql\"\nDATE=$(date +%m_%d_%Y)\nBACKUP_FILE=\"$BACKUP_DIR/${DB_NAME}_${DATE}.sql.gz\"\n\n# RUN MYSQL DUMP + COMPRESS\nmysqldump --no-tablespaces -u \"$DB_USER\" -p\"$DB_PASSWORD\" \"$DB_NAME\" | gzip &gt; \"$BACKUP_FILE\"\n\n# REMOVE BACKUPS OLDER THAN 30 DAYS\nfind \"$BACKUP_DIR\" -type f -name \"*.sql.gz\" -mtime +30 -delete\n</code></pre> Then, make the script executable <pre><code>sudo chmod +x /usr/local/bin/saln_app_db_backup.sh \n</code></pre></p>"},{"location":"DOSetup/#crontab","title":"Crontab","text":"<p>This part sets up the cron job for scheduled tasks:</p> <ul> <li>5-day old SALN Form cleanup</li> <li>Daily database backups</li> </ul> <pre><code>crontab -e\n</code></pre> <p>Then put these lines on the bottom of the file <pre><code>* * * * * cd /var/www/SALN-App/saln-server &amp;&amp; php artisan schedule:run &gt;&gt; /dev/null 2&gt;&amp;1\n0 2 * * * /usr/local/bin/mysql_backup.sh &gt;&gt; /var/log/mysql_backup.log 2&gt;&amp;1\n</code></pre></p>"},{"location":"DOSetup/#nginx-setup","title":"NGINX Setup","text":""},{"location":"DOSetup/#installing-nginx","title":"Installing NGINX","text":"<p>Install NGINX <pre><code>sudo apt install nginx -y\n</code></pre></p> <p>Then, we want nginx to work on port 80, but apache2 might already be in there. So, we disable apache2 since we have no use for it in this app. <pre><code>sudo ss -tulpn | grep :80\nsudo service apache2 stop\nsudo systemctl disable apache2\nsudo ss -tulpn | grep :80\nsudo service nginx start\nsudo service nginx status\n</code></pre></p>"},{"location":"DOSetup/#nginx-config","title":"NGINX Config","text":"<p>For the client side routing, we do two things. On the browser, we listen on port <code>80</code>.  But, since our MarkoJS client is being served on port <code>3000</code> by a Node server, we proxy it over to port <code>3000</code>.  If we see <code>/api/</code>, we proxy it over to port <code>82</code>, where the Laravel server-side logic is.  Additionally, this is declares the SSL certificates for port <code>80</code>, which allows HTTPS protocol. <pre><code>sudo nano /etc/nginx/sites-available/saln-client\n</code></pre> <pre><code>server {\n    listen 80;\n    server_name lamig-saln.online www.lamig-saln.online;\n\n    # Redirect all HTTP to HTTPS\n    return 301 https://$host$request_uri;\n}\n\nserver {\n    listen 443 ssl;\n    server_name lamig-saln.online www.lamig-saln.online;\n\n    ssl_certificate /etc/letsencrypt/live/lamig-saln.online/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/lamig-saln.online/privkey.pem;\n    include /etc/letsencrypt/options-ssl-nginx.conf;\n    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;\n\n    # --------- MARKOJS FRONTEND ---------\n    location / {\n        proxy_pass http://127.0.0.1:3000/;  # Node MarkoJS server\n        proxy_http_version 1.1;\n        proxy_set_header Upgrade $http_upgrade;\n        proxy_set_header Connection 'upgrade';\n        proxy_set_header Host $host;\n        proxy_cache_bypass $http_upgrade;\n    }\n\n    # --------- LARAVEL BACKEND ---------\n    location /api/ {\n        proxy_pass http://127.0.0.1:82;\n        proxy_http_version 1.1;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n    }\n\n    # Optional: serve Laravel static assets correctly\n    location /storage/ {\n        root /var/www/SALN-App/saln-server/public;\n    }\n\n    # Protect hidden files\n    location ~ /\\.(?!well-known).* {\n        deny all;\n    }\n}\n</code></pre></p> <p>For the server side routing: <pre><code>sudo nano /etc/nginx/sites-available/saln-server\n</code></pre> <pre><code>server {\n    listen 82;\n    server_name _;\n\n    root /var/www/SALN-App/saln-server/public;\n    index index.php index.html;\n\n    location / {\n        try_files $uri $uri/ /index.php?$query_string;\n    }\n\n    location ~ \\.php$ {\n        include snippets/fastcgi-php.conf;\n        fastcgi_pass unix:/run/php/php8.4-fpm.sock;\n        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;\n        include fastcgi_params;\n    }\n\n    location ~ /\\.(?!well-known).* {\n        deny all;\n    }\n}\n</code></pre></p> <p>Then, add syslinks to <code>sites-enabled</code>: <pre><code>sudo ln -s /etc/nginx/sites-available/saln-client /etc/nginx/sites-enabled\nsudo ln -s /etc/nginx/sites-available/saln-server /etc/nginx/sites-enabled\n</code></pre></p> <p>For checking nginx logs: open <code>/var/log/nginx/error.log</code> \\ To clear nginx logs: <code>sudo trunctate -s 0 /var/log/nginx/error.log</code></p> <p>ALWAYS run <code>sudo systemctl reload nginx</code> when making changes to the nginx config files. </p>"},{"location":"DOSetup/#ssl-certificate","title":"SSL Certificate","text":"<p>Do notice that the NGINX Config for client has SSL certificates. For the sake of documentation, this is how we got them.</p> <p>This assumes you have <code>snap</code> installed. Try <code>snap --version</code>.</p> <pre><code>snap install --classic certbot\ncertbot --nginx\nsudo systemctl reload nginx\n</code></pre>"},{"location":"DevInstallation/","title":"Development Installation","text":"<p>This setup is to be done for developers' machines.</p> <p>This assumes you have:</p> <ul> <li>A GitHub Account. This is where the repository is hosted online. You may be added as a Collaborator so that you could add changes to the main repository.</li> <li>Linux (or WSL if you're on Windows)</li> <li><code>saln-server/.env</code> gotten from the developers. This has configurations for database and mailer.</li> </ul>"},{"location":"DevInstallation/#git","title":"Git","text":""},{"location":"DevInstallation/#installing-git","title":"Installing Git","text":"<p>Assuming you do not have git on your machine yet, you may want to install it for your terminal. Then, we could also have your machine use your github account. <pre><code>sudo apt install git\ngit config --global user.name \"&lt;GITHUB_USERNAME&gt;\"\ngit config --global user.email \"&lt;GITHUB_EMAIL&gt;\"\n</code></pre></p> <p>Then, we recommend you connect to your GitHub account via SSH so that you won't need to enter your password every time. Here is GitHub's documentation for that: </p> <ul> <li>Generating a new SSH Key and adding it to the ssh-agent</li> <li>Adding a new SSH key to your GitHub account</li> </ul>"},{"location":"DevInstallation/#cloning-repository","title":"Cloning Repository","text":"<p>With your GitHub and Git now set up, you could now clone the repository into your machine: <pre><code>git clone git@github.com:SHIROKAMIQQ/SALN-App.git\n</code></pre></p> <p>Remember to put in <code>saln-server/.env</code></p> <p>NOTE: Us developers follow a workflow when creating new updates/features. Initially, you would have your code on a feature branch. Then, once that feature is done, you would make a Pull Request for it to be reviewed by the Lead Developer and then merged with the main branch. </p> <p>NOTE: The PRODUCTION branch contains the version to be used by the Digital Ocean virtual machine for production.</p>"},{"location":"DevInstallation/#for-client-side","title":"For Client Side","text":""},{"location":"DevInstallation/#installing-npm-and-nodejs","title":"Installing npm and Node.js","text":"<p>This section would need <code>npm</code>, the package manager of Node.js. <pre><code># Download and install nvm:\ncurl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash\n\n# in lieu of restarting the shell\n\\. \"$HOME/.nvm/nvm.sh\"\n\n# Download and install Node.js:\nnvm install 24\n\n# Verify the Node.js version:\nnode -v # Should print \"v24.13.0\".\n\n# Verify npm version:\nnpm -v # Should print \"11.6.2\".\n</code></pre></p>"},{"location":"DevInstallation/#installing-nodejs-dependencies","title":"Installing Node.js Dependencies","text":"<p>With npm now installed, we could now install the Node.js dependencies for the client-side. <pre><code>cd saln-client\nnpm install\n</code></pre></p>"},{"location":"DevInstallation/#running-client","title":"Running Client","text":"<p><code>npm run dev</code> must be run in the <code>saln-client</code> directory. <pre><code>cd saln-client\nnpm run dev\n</code></pre></p> <p>You should see a Marko page at (usually) http://localhost:3000</p>"},{"location":"DevInstallation/#for-server-side","title":"For Server Side","text":""},{"location":"DevInstallation/#setup-mysql","title":"Setup MySQL","text":"<p>The database of the app uses MySQL. You may install it via:  <pre><code>sudo apt install mysql-server -y\n</code></pre></p> <ul> <li>To start MySQL Server: <code>sudo service mysql start</code> </li> <li>To check status: <code>sudo service mysql status</code> </li> <li>To login locally: <code>sudo mysql -u root -p</code> </li> <li>To stop MySQL Server: <code>sudo service mysql stop</code></li> </ul> <p>On mysql, create a database for Laravel to connect to. We will also create a user for Laravel to use. As you could notice, these are also reflected in the <code>saln-server/.env</code> file from the developers.  <pre><code>CREATE DATABASE saln_app_DB;\nCREATE USER 'saln_user'@'localhost' IDENTIFIED BY 'saln_password';\nGRANT ALL PRIVILEGES ON saln_app_DB.* TO 'saln_user'@'localhost';\nFLUSH PRIVILEGES;\n</code></pre></p>"},{"location":"DevInstallation/#installing-laravel","title":"Installing Laravel","text":"<p>The app uses the Laravel framework. So, we would need PHP (at least version 8.4) and PHP's package manager, Composer before we install Laravel. <pre><code>sudo apt install -y software-properties-common\nsudo add-apt-repository ppa:ondrej/php -y\nsudo apt update\nsudo apt install php8.4 php8.4-cli php8.4-common php8.4-mbstring php8.4-xml php8.4-curl php8.4-mysql php8.4-zip php8.4-gd php8.4-intl php8.4-fpm php8.4-bcmath unzip -y\nsudo apt install composer -y\n</code></pre></p> <p>Now to install the Laravel Framework: <pre><code>composer global require laravel/installer\necho 'export PATH=\"$PATH:$HOME/.config/composer/vendor/bin\"' &gt;&gt; ~/.bashrc\nsource ~/.bashrc\n</code></pre></p> <p>We would also need to install other PHP packages needed for the app: <pre><code>cd saln-server\ncomposer install\n</code></pre></p> <p>To create all the tables from the <code>database/migrations</code> folder, or in the case you have database schema changes in <code>database/migrations</code>, run: <pre><code>php artisan migrate:fresh\n</code></pre></p>"},{"location":"DevInstallation/#cronjob-setup","title":"Cronjob setup","text":"<p>This part sets up the cron job for scheduled tasks (like deleting 5-day old SALN Forms).</p> <p>Run this to open up a text editor in your terminal which will edit the cron job file: <pre><code>crontab -e\n</code></pre></p> <p>Then put on the bottom of the file <pre><code>* * * * * cd /Path/to/SALN-App/saln-server &amp;&amp; php artisan schedule:run &gt;&gt; /dev/null 2&gt;&amp;1\n</code></pre></p> <p>Then to exit: <code>Ctrl+O</code>, <code>ENTER</code>, <code>CtrlX</code>. To test if the scheduler works, run <code>php artisan schdule:run</code>, or more specifically, <code>php artisan saln:cleanup</code>.  </p>"},{"location":"DevInstallation/#run-server","title":"Run Server","text":"<p><code>php artisan serve</code> must be run inside the <code>saln-server</code> folder. <pre><code>cd saln-server\nphp artisan serve\n</code></pre></p> <p>You should see some Laravel screen at (usually) http://127.0.0.1:8000</p>"},{"location":"ProdUpdate/","title":"Updating Production","text":"<p>This assumes you already have a fully set up Digital Ocean Droplet as per the Digital Ocean Setup page. </p> <p>You should also get these from the developers: </p> <ul> <li><code>saln-server/.env</code></li> <li>private <code>saln_ssh</code> and public <code>saln_ssh.pub</code> SSH key pair from the developers. Save these in <code>~/.ssh</code>.</li> </ul>"},{"location":"ProdUpdate/#pulling-the-repository","title":"Pulling the repository","text":"<p>Using git, we will clone the repository code. </p> <p>By convention, the <code>PRODUCTION</code> branch of the repository shall contain the current version to be used for production. Also by convention, the source code shall live in the <code>/var/www</code> directory. <pre><code>cd /var/www\ngit clone https://github.com/SHIROKAMIQQ/SALN-App.git\ncd SALN-App\ngit checkout PRODUCTION\n</code></pre> Now, you should have <code>/var/www/SALN-App</code> directory.</p>"},{"location":"ProdUpdate/#source-code-configuration-for-production","title":"Source Code Configuration for PRODUCTION","text":"<p>In <code>SALN-App/saln-client/src/api/config.js</code>, we want to edit the <code>API_BASE_URL</code> that the client side will use to communicate with the Laravel server-side. <pre><code>// FOR PROD:\nexport const API_BASE_URL = \"https://lamig-saln.online/api\";\n</code></pre></p> <p>In <code>SALN-App/saln-server/.env</code>, update <code>APP_URL</code>, <code>DB_PASSWORD</code>, and the <code>MAIL_</code> variables. There are comments in the <code>.env</code> file that show the correct values for these for the PRODUCTION server.</p>"},{"location":"ProdUpdate/#serving-client-side-code","title":"Serving Client-Side Code","text":""},{"location":"ProdUpdate/#installing-node-dependencies","title":"Installing Node Dependencies","text":"<p>Since we already have <code>npm</code> installed on the virtual machine, we could simply install the client-side's Node dependencies: <pre><code>cd /var/www/SALN-App/saln-client\nnpm install\n</code></pre></p>"},{"location":"ProdUpdate/#building-clients-static-code","title":"Building Client's Static Code","text":"<p>Normally in a development environment, we simply run <code>npm run dev</code>. This is just for the dev environment since browsers need to serve static JS files.</p> <p>So instead, to get the static js files of the current MarkoJS code of <code>saln-client</code>, we run: <pre><code>npm run build\n</code></pre> The output will be in <code>saln-client/dist</code>.</p>"},{"location":"ProdUpdate/#process-manager","title":"Process Manager","text":"<p>For this setup, we have the MarkoJS client served by a Node server. This is usually done by <code>npm run build</code> then <code>node dist/index.mjs</code>. But, this disconnects once the terminal gets terminated.  Process manager 2 (<code>pm2</code>) allows us to keep this process alive. <pre><code>pm2 start dist/index.mjs --name saln-client --max-memory-restart 200M\npm2 save\npm2 startup\n</code></pre></p> <p>Additionally, you have these tools for checking logs and active processes from pm2: <code>pm2 list</code>, <code>pm2 logs saln-client</code>, <code>pm2 status</code>.</p>"},{"location":"ProdUpdate/#serving-server-side-code","title":"Serving Server-Side Code","text":""},{"location":"ProdUpdate/#installing-php-dependencies","title":"Installing PHP dependencies","text":"<p>We should already have <code>composer</code> installed. So all that's left to do is install the PHP dependencies that the server-side code uses. <pre><code>cd /var/www/SALN-App/saln-server\ncomposer install\n</code></pre></p>"},{"location":"ProdUpdate/#in-case-of-server-side-code-changes-optional","title":"In Case of Server-Side Code Changes (Optional)","text":"<p>In the event that the new PRODUCTION code has changes to the serverside code (like logic or configuration), you might want to run these: <pre><code>php artisan config:clear\nphp artisan cache:clear\n</code></pre></p> <p>In the event that you have changes to the database schema (<code>database/migrations</code>) or just want to completely restart the database: <pre><code>php artisan migrate:fresh\n</code></pre></p> <p>In the event you want to change the key <code>APP_KEY</code> from <code>.env</code> used by <code>Crypt</code>, run: <pre><code>php artisan key:generate\n</code></pre></p>"},{"location":"ProdUpdate/#mysql-server","title":"MySQL Server","text":"<p>Though the entire MySQL should already be setup from the Digital Ocean Setup page, you might want to keep note of these commands to do some sanity checks for the MySQL server:</p> <ul> <li>To start MySQL Server: <code>sudo service mysql start</code></li> <li>To check status: <code>sudo service mysql status</code> </li> <li>To login locally: <code>sudo mysql -u root -p</code> </li> <li>To stop MySQL Server: <code>sudo service mysql stop</code></li> </ul> <p>From here, you could do MySQL queries directly to the database. Do note though that the source code uses the <code>Crypt</code> library of Laravel to encrypt stored database entries.</p>"},{"location":"ProdUpdate/#nginx-permissions","title":"NGINX Permissions","text":"<p>We need to give nginx (www-data group) permissions to read/traverse through SALN-App. We also want to give Laravel write permissions for the saln-server's logs and cache. <pre><code>sudo chown -R www-data:www-data /var/www/SALN-App\n\n# Directories: readable + traversable\nsudo find /var/www/SALN-App -type d -exec chmod 755 {} \\;\n# Files: readable\nsudo find /var/www/SALN-App -type f -exec chmod 644 {} \\;\n\n# .env is readable only by www-data\nsudo chmod 600 /var/www/SALN-App/saln-server/.env\n\n# Give Laravel write access to storage (logs) and cache\nsudo chmod -R 775 /var/www/SALN-App/saln-server/storage\nsudo chmod -R 775 /var/www/SALN-App/saln-server/bootstrap/cache\n</code></pre></p> <p>ALWAYS run <code>sudo systemctl reload nginx</code> when making changes to the nginx config files. </p>"},{"location":"UserManual/","title":"User Manual","text":"<p>This is the user manual of the app. This includes all the use cases of the app. </p> <p>The app could be accessed via https://lamig-saln.online.</p> <p>It could also be accessed offline after accessing the website and also be installed as a Progressive Web App.</p>"},{"location":"UserManual/#login-and-otp-verification","title":"Login and OTP Verification","text":"<p>In the event you have not yet logged into an account, you would start off on the login page, which asks you for an email to associate your account with.</p> <p></p> <p>After entering an email, you would be sent to a page asking for an OTP, which is composed of four concatenated words.</p> <p></p> <p>This OTP is seen in the inbox of the entered email. This is sent from <code>saln_app@mg.lamig-saln.online</code></p> <p></p> <p>This sends you to the dashboard.</p>"},{"location":"UserManual/#dashboard","title":"Dashboard","text":"<p>This is the dashboard. If you are already logged in, then going to <code>https://lamig-saln.online</code> will immediately send you here.</p> <p>It has actions for the account and actions per SALN Form, all of which will be covered in the following sections.</p>"},{"location":"UserManual/#log-out","title":"Log Out","text":"<p>On the dashboard, pressing the Log Out button will log you out of your account and then send you back to the login page. This does not delete anything from the server's database.</p>"},{"location":"UserManual/#delete-account","title":"Delete Account","text":"<p>On the dashboard, pressing the Delete Account button will log you out of your account and then send you back to the login page. Your email and its SALN Forms will be deleted from the server's database.</p>"},{"location":"UserManual/#accomplish-new-saln","title":"Accomplish New SALN","text":"<p>On the dashboard, pressing the Accomplish New SALN button will send you to a digitilized version of the physical SALN Form. The user could fill this form out and then save it. Upon saving it, the user will be sent back to the dashboard and the new entry will be seen in his list of SALN Forms.</p>"},{"location":"UserManual/#upload-saln-json","title":"Upload SALN JSON","text":"<p>On the dashboard, pressing the Upload JSON button will send you to a method of entering SALN Form data via a JSON file. </p> <p> The format of the JSON file could be seen in the Help tab.</p>"},{"location":"UserManual/#export-saln-pdf","title":"Export SALN PDF","text":"<p>Clicking the PDF button for a SALN Form on the dashboard will export that SALN Form's data placed into a PDF version of the original SALN Form. </p> <p></p>"},{"location":"UserManual/#export-saln-json","title":"Export SALN JSON","text":"<p>Clicking the JSON button for a SALN Form on the dashboard will export that SALN Form's data as a JSON file.</p> <p></p>"},{"location":"UserManual/#edit-saln-form","title":"Edit SALN Form","text":"<p>Clicking the EDIT button for a SALN Form will send you to the digital version of the SALN Form with its entries already there. You could edit fields in the digital SALN Form and then save it.</p>"},{"location":"UserManual/#delete-saln-form","title":"Delete SALN Form","text":"<p>Clicking the DEL button for a SALN Form will delete it from the server's database.  </p>"},{"location":"UserManual/#offline-mode","title":"Offline Mode","text":"<p>With the use of a Service Worker, the developers have made it so that the website is cached as you use it. This means that you have some functionality even though you are offline. These functions include:</p> <ul> <li>Accomplishing a new SALN Form</li> <li>Uploading a SALN JSON</li> <li>Editing a SALN Form</li> <li>Exporting a SALN From as a PDF</li> <li>Exporting a SALN Form as a JSON</li> <li>Deleting a SALN Form</li> <li>Logging Out</li> </ul> <p>You could do these functions offline. Then once you go back online, the Service Worker will resend the API calls you made to the server.</p> <p>Do note that in the case of multiple offline updates to a single SALN Form (case of multiple devices), the update with the latest <code>updatedAt</code> timestamp will prevail. </p>"},{"location":"UserManual/#install-pwa","title":"Install PWA","text":"<p>Installing the app as a Progressive Web App could be done via the install button of your browser when accessing any page on <code>https://lamig-saln.online</code>. </p> <p>This install button is typically found on the search bar. For example below, Microsoft Edge is being used. </p> <p></p>"}]}